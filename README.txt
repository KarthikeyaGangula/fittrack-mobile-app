FitTrack - Separate Styles Setup

Rename/copy the files into your Expo project as follows:
1. FitTrack_App.js -> App.js
2. FitTrack_Styles.js -> Styles.js

Both files must be in the same folder.

App.js imports the styles using:
import styles from "./Styles";

Then run:
npx expo start --tunnel -c
